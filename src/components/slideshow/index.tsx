import { FC } from "react";
import Slider from "./slider";

const Slideshow: FC = () => {
  return (
    <div className="px-4 w-full">
      {" "}
      <Slider />
    </div>
  );
};

export default Slideshow;
